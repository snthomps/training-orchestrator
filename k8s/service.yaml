apiVersion: v1
kind: Service
metadata:
  name: training-orchestrator
  namespace: training
spec:
  selector:
    app: training-orchestrator
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
